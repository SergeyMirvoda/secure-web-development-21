html
    head
        link(rel="stylesheet",href="https://unpkg.com/@picocss/pico@latest/css/pico.min.css")
        title Книги в библиотеке
        
    body
        h1 Список книг
        form(action='/books', method='GET')
            span(id='lblFilter') #{filter}
            span #{filter}
            p
            | Фильтр по названию: 
            input(type='text', name='name', value='')
            input(type='submit', value='Применить')
            
        table(style='width:100%', border='1', role="grid")
            thead
                tr
                th Id
                th Автор
                th Книга
            tbody
            each book in data
                tr
                    td #{book.id}
                    td #{book.author}
                    td #{book.book}
        script.
            //var u = new URL(window.location);
            //var filteredBy = u.searchParams.get("name");
            //if(filteredBy){
            //    var f = document.getElementById('lblFilter');
            //    f.innerHTML = filteredBy;
            //}

            if(filteredBy.Contains('<'))
                return;

            var f = document.getElementById('lblFilter');
            f.innerHTML ='<b>#{filter}</b>';
            